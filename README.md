# Happiness Tracker – Приложение для оценки эмоционального состояния

**Happiness Tracker** — это простое, но мощное приложение на Flutter, позволяющее отслеживать ваше эмоциональное состояние через оценку повседневных событий и калибровку личной шкалы счастья на основе эталонных жизненных ситуаций.

---

## 📌 Основные функции

- **Калибровка личной шкалы эмоций**:  
  Вы оцениваете эталонные события (например, «рождение ребёнка» или «потеря работы») по шкале от -100 до +100. Это позволяет приложению определить вашу индивидуальную «максимальную амплитуду» эмоций (`x`), которая затем используется для оценки повседневных событий.

- **Добавление ежедневных событий**:  
  Вы можете фиксировать любые события дня и оценивать их в диапазоне от `-x` до `+x`, где `x` — ваша персональная максимальная оценка, вычисленная из эталонов.

- **Визуализация динамики состояния**:  
  Приложение строит график кумулятивной суммы оценок по дням (`F(t) = Σ rating`), что помогает увидеть общую тенденцию вашего эмоционального фона со временем.

- **Локальное хранение данных**:  
  Все данные сохраняются на устройстве с помощью `shared_preferences` — никаких облаков, аккаунтов или интернета не требуется.

---

## 📱 Интерфейс

- **Главная страница**:  
  - Кнопка «Пройти тест эталонов» — настройка вашей эмоциональной шкалы.  
  - Кнопка «Добавить событие дня» — запись нового события.  
  - Кнопка «Статистика» — просмотр графика и истории.  
  - Отображение текущего масштаба `x` и списка эталонных событий с вашими оценками.

- **Тест эталонов**:  
  Слайдеры для оценки 5 предопределённых событий (можно расширить).

- **Добавление события**:  
  Поле ввода названия + слайдер в диапазоне `[-x, +x]`.

- **Статистика**:  
  - График кумулятивной суммы (линейный, с точками по дням).  
  - Среднее значение `F(t)` за всё время.  
  - Список всех событий с датами и оценками.

---

## 🛠 Технические детали

### Зависимости (`pubspec.yaml`)
```yaml
dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15
  fl_chart: ^0.55.1
```

### Модели данных
- `ReferenceEvent` — эталонное событие с `id`, `title` и `rating` (по умолчанию 0).
- `DailyEvent` — событие дня с `id`, `date`, `title` и `rating`.

### Хранение
- Эталонные события: ключ `ref_events_v1`
- Ежедневные события: ключ `daily_events_v1`
- Формат: JSON, сериализованный в строку через `shared_preferences`.

### Алгоритмы
- **Масштаб `x`**: вычисляется как максимальное абсолютное значение среди оценок эталонных событий (минимум 10.0).
- **График**: кумулятивная сумма ежедневных оценок, сгруппированных по дате (без времени).

---

## 📥 Установка и запуск

1. Создайте новый Flutter-проект:
   ```bash
   flutter create happiness_tracker
   cd happiness_tracker
   ```

2. Замените содержимое `lib/main.dart` на приведённый выше код.

3. Добавьте зависимости в `pubspec.yaml` (см. выше).

4. Выполните:
   ```bash
   flutter pub get
   flutter run
   ```

---

## 💡 Идеи для расширения

- Экспорт/импорт данных (через файл или QR-код).
- Редактирование и удаление событий.
- Настройка собственных эталонных событий.
- Уведомления напоминания о ежедневной оценке.
- Темная тема и кастомизация UI.

---

> **Примечание**: Приложение полностью автономно и не передаёт данные в сеть. Все оценки хранятся только на вашем устройстве.

---

Создано для осознанного отслеживания эмоционального состояния и личностного роста.
